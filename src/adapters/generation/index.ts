/**
 * Generation adapter: calls headless `claude`, implements `HandoffGenerator` in the lean and
 * deep variants (D-011). See docs/ARQUITETURA.md.
 *
 * Still empty — comes in at S2-T2.
 */
export {};
