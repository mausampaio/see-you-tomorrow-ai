# CI do see-you-tomorrow. Roda o portão de qualidade (`npm run verificar`: tipos, lint,
# dependency-cruiser, build e testes de unidade + integração com cobertura) nos 3 SOs
# suportados. Ver docs/PLANO-DE-ENTREGA.md S0-T2 e AGENTS.md.
#
# Não roda test:e2e nem test:contrato aqui: e2e é caro e contrato depende do ~/.claude real da
# máquina (AGENTS.md, docs/TESTES.md).
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verificar:
    name: verificar (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - name: Baixa o código
        uses: actions/checkout@v4

      - name: Configura o Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Instala as dependências
        run: npm ci

      - name: Roda o portão de qualidade (tipos + lint + dependency-cruiser + build + cobertura)
        run: npm run verificar

      # TEMP (S2-T8): reroda só a suíte com cobertura em modo verbose, para medir a duração real de
      # cada teste no runner antes de fixar um novo orçamento em
      # tests/integration/process/termination.test.ts. Removido depois de coletar a medição.
      - name: '[TEMP S2-T8] Medição verbose da suíte'
        run: npx vitest run --project unit --project integration --project guards --reporter=verbose
